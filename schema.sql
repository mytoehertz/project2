CREATE DATABASE fleshEaters;

USE fleshEaters;


-- Can run the commands below to make sure sequelize is working properly, once the file is wondering. Run these commands in your workbench, AFTER our file is running and AFTER you've set up the database:

select * from students;

-- should reveal one test student in the students table


select * from counselors;

-- ditto for above, but for counselors table

select * from conversations;

-- ditto for above, but for conversations table.

